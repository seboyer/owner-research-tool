[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python scheduler.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# Health check — logs are the health signal for this service
healthcheckPath = ""
